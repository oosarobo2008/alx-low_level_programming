#include <stdio.h>

/**
* main - count 1 2 3  , as par nursery svchool pikin wey i be na
* Return: Always 0 (success)
*/

int main(void)
{
int pay;
for (pay = 0; pay <= 10; pay++)
for (pay = 0; pay <= 9; pay++)
{
printf("%d", pay);
}
printf("\n");
return (0);
}
