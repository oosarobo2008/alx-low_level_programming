#include <stdio.h>
/**
* main - make i count one two three like nursery school pikin
* Return: Always 0 (success)
*/

int main(void)
{
char ch ='0';
for (ch = '0'; ch <= '9'; ch++)
putchar(ch);
putchar(\n);
return (0);
}
